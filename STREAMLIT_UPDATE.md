# Streamlit Version Update

Updated the Streamlit version requirement in `requirements.txt` from:
```
streamlit>=1.32.0
```
to:
```
streamlit>=1.56.0
```

This ensures the application uses at least version 1.56.0, which is the version tested with the recent enhancements for serials bibliographic data handling.

The application has been verified to start and run correctly with this version constraint.

Update performed: June 10, 2026